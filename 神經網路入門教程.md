# 神經網路入門完整教程 - Windows環境

## 目錄
1. [什麼是神經網路？](#什麼是神經網路)
2. [Windows環境準備](#windows環境準備)
3. [Python安裝與設定](#python安裝與設定)
4. [必要套件安裝](#必要套件安裝)
5. [第一個神經網路程式](#第一個神經網路程式)
6. [實戰練習項目](#實戰練習項目)
7. [常見問題解決](#常見問題解決)
8. [進階學習資源](#進階學習資源)

## 什麼是神經網路？

神經網路是一種模仿人腦神經元工作方式的電腦程式。就像人腦中的神經元會接收信號、處理信息，然後傳遞給下一個神經元一樣，人工神經網路也是由許多相互連接的「節點」組成。

### 簡單比喻
想像神經網路就像一個智慧的過濾器：
- **輸入**：給它一張照片
- **處理**：神經網路分析照片的每個像素
- **輸出**：告訴你這是貓還是狗

### 神經網路的基本組成
1. **輸入層**：接收原始資料（如圖像、文字）
2. **隱藏層**：進行資料處理和特徵提取
3. **輸出層**：產生最終結果

## Windows環境準備

### 系統需求
- Windows 10 或 Windows 11
- 至少 8GB RAM（建議 16GB 以上）
- 至少 20GB 可用磁碟空間
- 穩定的網路連線

### 建議的硬體配置
- **CPU**：Intel i5 或 AMD Ryzen 5 以上
- **GPU**：NVIDIA GTX 1060 或更新（可選，但會大幅提升訓練速度）
- **記憶體**：16GB 或以上

## Python安裝與設定

### 步驟1：下載Python
1. 前往 [Python官方網站](https://www.python.org/downloads/)
2. 下載最新的 Python 3.9-3.11 版本（建議 3.10）
3. **重要**：下載時選擇 "Windows installer (64-bit)"

### 步驟2：安裝Python
1. 執行下載的安裝檔
2. **務必勾選** "Add Python to PATH"
3. 選擇 "Customize installation"
4. 確保勾選：
   - pip
   - tcl/tk and IDLE
   - Python test suite
   - py launcher
5. 點擊 "Install"

### 步驟3：驗證安裝
開啟命令提示字元（cmd），輸入：
```cmd
python --version
pip --version
```
如果顯示版本號，代表安裝成功。

## 必要套件安裝

### 創建虛擬環境（強烈建議）
```cmd
# 創建虛擬環境
python -m venv neural_network_env

# 啟動虛擬環境
neural_network_env\Scripts\activate

# 你會看到提示符前面出現 (neural_network_env)
```

### 安裝核心套件
```cmd
# 更新pip
python -m pip install --upgrade pip

# 安裝基礎套件
pip install numpy pandas matplotlib

# 安裝機器學習套件
pip install scikit-learn

# 安裝深度學習框架（選擇其一）
pip install tensorflow  # Google的TensorFlow（推薦給初學者）
# 或
pip install torch torchvision  # Facebook的PyTorch

# 安裝Jupyter Notebook（可視化開發環境）
pip install jupyter
```

### 驗證安裝
創建測試檔案 `test_installation.py`：
```python
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
try:
    import tensorflow as tf
    print(f"TensorFlow 版本: {tf.__version__}")
except ImportError:
    print("TensorFlow 未安裝")

try:
    import torch
    print(f"PyTorch 版本: {torch.__version__}")
except ImportError:
    print("PyTorch 未安裝")

print("所有基礎套件安裝成功！")
```

執行測試：
```cmd
python test_installation.py
```

## 第一個神經網路程式

我們將創建一個簡單的神經網路來識別手寫數字（MNIST資料集）。

### 使用TensorFlow/Keras的範例
創建檔案 `first_neural_network.py`：

```python
# 導入必要的套件
import tensorflow as tf
from tensorflow import keras
import numpy as np
import matplotlib.pyplot as plt

# 設定隨機種子，確保結果可重現
tf.random.set_seed(42)
np.random.seed(42)

print("開始載入資料...")
# 載入MNIST手寫數字資料集
(x_train, y_train), (x_test, y_test) = keras.datasets.mnist.load_data()

print(f"訓練資料形狀: {x_train.shape}")
print(f"測試資料形狀: {x_test.shape}")

# 資料預處理
# 將像素值從0-255縮放到0-1之間
x_train = x_train.astype('float32') / 255.0
x_test = x_test.astype('float32') / 255.0

print("開始建立神經網路模型...")
# 建立神經網路模型
model = keras.Sequential([
    keras.layers.Flatten(input_shape=(28, 28)),  # 將28x28圖像展平成784個數字
    keras.layers.Dense(128, activation='relu'),   # 隱藏層，128個神經元
    keras.layers.Dropout(0.2),                   # 防止過度擬合
    keras.layers.Dense(10, activation='softmax') # 輸出層，10個數字類別
])

# 編譯模型
model.compile(
    optimizer='adam',                    # 優化器
    loss='sparse_categorical_crossentropy', # 損失函數
    metrics=['accuracy']                 # 評估指標
)

# 顯示模型結構
model.summary()

print("開始訓練模型...")
# 訓練模型
history = model.fit(
    x_train, y_train,
    epochs=5,           # 訓練5個週期
    batch_size=32,      # 批次大小
    validation_split=0.2, # 使用20%資料做驗證
    verbose=1           # 顯示訓練過程
)

print("開始評估模型...")
# 評估模型
test_loss, test_accuracy = model.evaluate(x_test, y_test, verbose=0)
print(f"\n測試準確率: {test_accuracy:.4f}")

# 預測範例
predictions = model.predict(x_test[:5])
predicted_classes = np.argmax(predictions, axis=1)
actual_classes = y_test[:5]

print("\n預測結果範例:")
for i in range(5):
    print(f"實際數字: {actual_classes[i]}, 預測數字: {predicted_classes[i]}")

# 視覺化訓練過程
plt.figure(figsize=(12, 4))

plt.subplot(1, 2, 1)
plt.plot(history.history['loss'], label='訓練損失')
plt.plot(history.history['val_loss'], label='驗證損失')
plt.title('模型損失')
plt.xlabel('週期')
plt.ylabel('損失')
plt.legend()

plt.subplot(1, 2, 2)
plt.plot(history.history['accuracy'], label='訓練準確率')
plt.plot(history.history['val_accuracy'], label='驗證準確率')
plt.title('模型準確率')
plt.xlabel('週期')
plt.ylabel('準確率')
plt.legend()

plt.tight_layout()
plt.savefig('training_results.png')
plt.show()

print("程式執行完成！檢查 training_results.png 查看訓練結果圖表。")
```

### 執行你的第一個神經網路
```cmd
# 確保虛擬環境已啟動
neural_network_env\Scripts\activate

# 執行程式
python first_neural_network.py
```

### 預期結果
程式執行後，你應該會看到：
1. 模型結構摘要
2. 訓練過程（5個週期）
3. 最終測試準確率（通常在97%以上）
4. 預測範例結果
5. 訓練過程圖表

## 實戰練習項目

### 項目1：貓狗分類器
創建 `cat_dog_classifier.py`：

```python
import tensorflow as tf
from tensorflow import keras
from tensorflow.keras.preprocessing.image import ImageDataGenerator
import numpy as np
import matplotlib.pyplot as plt
import os

# 檢查是否有GPU
print("可用的GPU:", tf.config.list_physical_devices('GPU'))

# 創建簡單的資料生成器（使用內建資料集）
print("準備資料...")

# 使用CIFAR-10資料集中的貓(類別3)和狗(類別5)
(x_train, y_train), (x_test, y_test) = keras.datasets.cifar10.load_data()

# 只保留貓和狗的資料
cat_dog_indices_train = np.where((y_train == 3) | (y_train == 5))[0]
cat_dog_indices_test = np.where((y_test == 3) | (y_test == 5))[0]

x_train_catdog = x_train[cat_dog_indices_train]
y_train_catdog = y_train[cat_dog_indices_train]
x_test_catdog = x_test[cat_dog_indices_test]
y_test_catdog = y_test[cat_dog_indices_test]

# 重新標記：貓=0, 狗=1
y_train_catdog = (y_train_catdog == 5).astype(int)
y_test_catdog = (y_test_catdog == 5).astype(int)

# 正規化
x_train_catdog = x_train_catdog.astype('float32') / 255.0
x_test_catdog = x_test_catdog.astype('float32') / 255.0

print(f"訓練資料: {x_train_catdog.shape}")
print(f"測試資料: {x_test_catdog.shape}")

# 建立CNN模型
model = keras.Sequential([
    keras.layers.Conv2D(32, (3, 3), activation='relu', input_shape=(32, 32, 3)),
    keras.layers.MaxPooling2D((2, 2)),
    keras.layers.Conv2D(64, (3, 3), activation='relu'),
    keras.layers.MaxPooling2D((2, 2)),
    keras.layers.Conv2D(64, (3, 3), activation='relu'),
    keras.layers.Flatten(),
    keras.layers.Dense(64, activation='relu'),
    keras.layers.Dropout(0.5),
    keras.layers.Dense(1, activation='sigmoid')  # 二元分類
])

model.compile(
    optimizer='adam',
    loss='binary_crossentropy',
    metrics=['accuracy']
)

model.summary()

# 訓練模型
print("開始訓練...")
history = model.fit(
    x_train_catdog, y_train_catdog,
    epochs=10,
    batch_size=32,
    validation_split=0.2,
    verbose=1
)

# 評估模型
test_loss, test_accuracy = model.evaluate(x_test_catdog, y_test_catdog, verbose=0)
print(f"測試準確率: {test_accuracy:.4f}")

# 視覺化結果
plt.figure(figsize=(12, 4))

plt.subplot(1, 2, 1)
plt.plot(history.history['loss'], label='訓練損失')
plt.plot(history.history['val_loss'], label='驗證損失')
plt.title('貓狗分類器 - 損失')
plt.xlabel('週期')
plt.ylabel('損失')
plt.legend()

plt.subplot(1, 2, 2)
plt.plot(history.history['accuracy'], label='訓練準確率')
plt.plot(history.history['val_accuracy'], label='驗證準確率')
plt.title('貓狗分類器 - 準確率')
plt.xlabel('週期')
plt.ylabel('準確率')
plt.legend()

plt.tight_layout()
plt.savefig('cat_dog_results.png')
plt.show()

print("貓狗分類器訓練完成！")
```

### 項目2：簡單的文字情感分析
創建 `sentiment_analysis.py`：

```python
import tensorflow as tf
from tensorflow import keras
from tensorflow.keras.preprocessing.text import Tokenizer
from tensorflow.keras.preprocessing.sequence import pad_sequences
import numpy as np

# 簡單的範例資料
texts = [
    "我很喜歡這部電影",
    "這個產品很棒",
    "服務態度很差",
    "非常失望",
    "超級推薦",
    "品質很好",
    "完全不值得",
    "很滿意這次購買",
    "太糟糕了",
    "物超所值"
]

# 情感標籤：1=正面, 0=負面
labels = [1, 1, 0, 0, 1, 1, 0, 1, 0, 1]

print("準備文字資料...")
# 文字預處理
tokenizer = Tokenizer(num_words=1000, oov_token="<OOV>")
tokenizer.fit_on_texts(texts)

sequences = tokenizer.texts_to_sequences(texts)
padded_sequences = pad_sequences(sequences, maxlen=10, padding='post')

print("文字向量化完成")
print("範例序列:", padded_sequences[0])

# 建立簡單的文字分類模型
model = keras.Sequential([
    keras.layers.Embedding(1000, 16, input_length=10),
    keras.layers.GlobalAveragePooling1D(),
    keras.layers.Dense(16, activation='relu'),
    keras.layers.Dropout(0.5),
    keras.layers.Dense(1, activation='sigmoid')
])

model.compile(
    optimizer='adam',
    loss='binary_crossentropy',
    metrics=['accuracy']
)

print("模型結構:")
model.summary()

# 訓練模型（因為資料很少，所以多訓練幾個週期）
print("開始訓練情感分析模型...")
history = model.fit(
    padded_sequences,
    np.array(labels),
    epochs=100,
    verbose=0
)

# 測試預測
test_texts = ["這個很不錯", "太失望了", "非常棒"]
test_sequences = tokenizer.texts_to_sequences(test_texts)
test_padded = pad_sequences(test_sequences, maxlen=10, padding='post')

predictions = model.predict(test_padded)

print("\n情感分析結果:")
for i, text in enumerate(test_texts):
    sentiment = "正面" if predictions[i][0] > 0.5 else "負面"
    confidence = predictions[i][0] if predictions[i][0] > 0.5 else 1 - predictions[i][0]
    print(f"文字: '{text}' -> {sentiment} (信心度: {confidence:.2f})")

print("情感分析模型完成！")
```

## 常見問題解決

### 問題1：ImportError: No module named 'tensorflow'
**解決方法：**
```cmd
# 確保虛擬環境已啟動
neural_network_env\Scripts\activate

# 重新安裝TensorFlow
pip uninstall tensorflow
pip install tensorflow
```

### 問題2：CUDA相關錯誤（GPU問題）
**解決方法：**
```python
# 在程式開頭添加以下程式碼，強制使用CPU
import os
os.environ['CUDA_VISIBLE_DEVICES'] = '-1'
```

### 問題3：記憶體不足錯誤
**解決方法：**
```python
# 減少批次大小
model.fit(..., batch_size=16)  # 從32改為16

# 或使用記憶體成長設定
import tensorflow as tf
gpus = tf.config.experimental.list_physical_devices('GPU')
if gpus:
    tf.config.experimental.set_memory_growth(gpus[0], True)
```

### 問題4：模型訓練太慢
**解決方法：**
1. 減少資料量進行測試
2. 減少模型複雜度
3. 使用較少的訓練週期
4. 考慮使用Google Colab或其他雲端平台

### 問題5：準確率太低
**解決方法：**
1. 增加訓練週期
2. 調整學習率
3. 增加更多資料
4. 嘗試不同的模型架構

## 進階學習資源

### 中文資源
1. **CSDN** - https://blog.csdn.net/
   - 搜尋關鍵字："深度學習 tensorflow keras"
   - 許多實戰教程和經驗分享

2. **GitHub中文項目**
   - [tensorflow-zh](https://github.com/jikexueyuanwiki/tensorflow-zh)
   - [deep-learning-with-python-cn](https://github.com/zhangqianhui/deep-learning-with-python-cn)

3. **bilibili視頻教程**
   - 搜尋："深度學習入門"、"神經網路教程"
   - 推薦UP主的完整課程系列

### 英文資源
1. **官方文檔**
   - [TensorFlow官網](https://www.tensorflow.org/)
   - [Keras官網](https://keras.io/)
   - [PyTorch官網](https://pytorch.org/)

2. **在線課程**
   - Coursera: Deep Learning Specialization
   - edX: MIT Introduction to Deep Learning
   - Udacity: Deep Learning Nanodegree

3. **書籍推薦**
   - "Deep Learning with Python" by François Chollet
   - "Hands-On Machine Learning" by Aurélien Géron

### 實戰項目建議
1. **初級項目**
   - MNIST手寫數字識別
   - 簡單圖像分類
   - 基礎文字分析

2. **中級項目**
   - 貓狗分類器
   - 情感分析系統
   - 股價預測模型

3. **高級項目**
   - 人臉識別系統
   - 自然語言處理應用
   - 生成對抗網路(GAN)

### 社群與論壇
1. **Stack Overflow** - 程式技術問題
2. **Reddit** - r/MachineLearning, r/tensorflow
3. **Discord/Telegram** - 各種AI學習群組

## 下一步建議

1. **完成基礎教程**：確保上述所有程式都能成功執行
2. **參與線上社群**：加入相關的學習群組
3. **動手做項目**：選擇一個感興趣的領域深入研究
4. **持續學習**：每週花時間學習新的概念和技術
5. **記錄學習過程**：建立自己的學習筆記和代碼庫

記住：神經網路學習是一個漸進的過程，不要急於求成。從簡單的範例開始，逐步增加複雜度，多動手實作，多看別人的代碼，你一定能夠掌握這項技術！

---

**最後提醒**：
- 保持程式執行環境整潔（使用虛擬環境）
- 定期備份重要的程式碼和模型
- 遇到問題時，先查閱官方文檔和錯誤訊息
- 多參與社群討論，分享學習心得

祝你學習順利！🚀